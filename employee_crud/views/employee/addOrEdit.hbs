<div class="mt-4">
    <h3 class="text-center mb-4">{{viewTitle}}</h3>
    <form action="/employee" method="POST" autocomplete="off">
        <div class="form-row">
            <div class="form-group col-md-6">
                <label for="fullname">Full Name</label>
                <input type="text" class="form-control" id="fullname" name="fullname" placeholder="Enter Fullname" value="{{employee.fullname}}">
                <div class="text-danger">
                    {{employee.FullNameError}}
                </div>
            </div>
            <div class="form-group col-md-6">
            <label for="email">E-mail</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Email" value="{{employee.email}}">
            </div>
        </div>
        <div class="form-group">
            <label for="mobile">Mobile</label>
            <input type="text" class="form-control" id="mobile" name="mobile" placeholder="Mobile" value="{{employee.mobile}}">
        </div>
        <div class="form-group">
            <label for="city">City</label>
            <input type="text" class="form-control" id="city" name="city" placeholder="City" value="{{employee.city}}">
        </div>
        
            <button type="submit" class="btn btn-primary">
                <i class="fa fa-database"></i>
                Submit
            </button>
    </form>
</div>